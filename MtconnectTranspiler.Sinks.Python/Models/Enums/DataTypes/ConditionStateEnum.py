class ConditionStateEnum(MTCEnum):
	
		Normal = "   {{term(condition state)}} that indicates operation within specified limits.   "
		
		Warning = "   {{term(condition state)}} that requires concern and supervision and may become hazardous if no action is taken.   "
		
		Fault = "   {{term(condition state)}} that requires intervention to continue operation to function properly.   "
		